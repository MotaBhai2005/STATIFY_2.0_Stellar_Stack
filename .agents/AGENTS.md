# Custom Agent Rules for Financial Analyst Chatbot

- **Python Interpreter**: Always use the virtual environment's Python interpreter located at `.venv\Scripts\python.exe` (or `.\.venv\Scripts\python.exe`) when running python commands, installing packages, testing, or executing scripts in this workspace. Do not use the global system Python.
